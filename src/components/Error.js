import React from 'react';

const Error = () => {
    return(
        <section id="content">
                <h2 className="subheader">Página no encontrada</h2>
                <p>La pagina a la intentas acceder no existe en la web</p>
        </section >
    );
}

export default Error;